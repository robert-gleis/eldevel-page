---
import { content } from '../content/de.ts';

// Helper function to build correct asset paths
const getAssetPath = (path: string) => {
  const baseUrl = import.meta.env.BASE_URL;
  // Remove trailing slash from baseUrl if it exists
  const cleanBaseUrl = baseUrl.endsWith('/') ? baseUrl.slice(0, -1) : baseUrl;
  return cleanBaseUrl + '/' + (path.startsWith('/') ? path.slice(1) : path);
};
---

<!-- Footer -->
<footer class="bg-gray-900 dark:bg-gray-950 text-white">
  <div class="container-custom py-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      
      <!-- Company Info -->
      <div class="space-y-4">
        <div class="flex items-center">
                           <img 
                   src={getAssetPath("images/eldevel-logo.png")} 
                   alt="eldevel – Electronics Development"
                   class="h-10 w-auto filter brightness-0 invert opacity-90 hover:opacity-100 transition-opacity"
                   loading="lazy"
                 />
        </div>
        <p class="text-gray-300">{content.footer.tagline}</p>
      </div>

      <!-- Quick Links -->
      <div class="space-y-4">
        <h4 class="font-semibold">Navigation</h4>
        <nav class="space-y-2">
          <a href="#unternehmen" class="block text-gray-300 hover:text-white transition-colors">
            {content.navigation.company}
          </a>
          <a href="#leistungen" class="block text-gray-300 hover:text-white transition-colors">
            {content.navigation.services}
          </a>
          <a href="#prozess" class="block text-gray-300 hover:text-white transition-colors">
            {content.navigation.process}
          </a>
          <a href="#kontakt" class="block text-gray-300 hover:text-white transition-colors">
            {content.navigation.contact}
          </a>
        </nav>
      </div>

      <!-- Contact -->
      <div class="space-y-4">
        <h4 class="font-semibold">Kontakt</h4>
        <div class="space-y-2 text-gray-300">
          <p>{content.contact.info.phone}</p>
          <p><a href="mailto:info@eldevel.de" class="hover:text-white transition-colors">{content.contact.info.email}</a></p>
          <p>{content.contact.info.hours}</p>
        </div>
      </div>
    </div>

    <div class="border-t border-gray-800 mt-12 pt-8 flex flex-col md:flex-row justify-between items-center">
      <p class="text-gray-400 text-sm">{content.footer.copyright}</p>
                     <div class="flex space-x-6 mt-4 md:mt-0">
                 <a href="/impressum" class="text-gray-400 hover:text-white text-sm transition-colors">
                   {content.footer.links.imprint}
                 </a>
                 <a href="/datenschutz" class="text-gray-400 hover:text-white text-sm transition-colors">
                   {content.footer.links.privacy}
                 </a>
               </div>
    </div>
  </div>
</footer>
